// funciones["nombre de la funcion"] = funcion a aplicar

funciones["identidad"] = x => x;
funciones["doble"] = x => x * 2;
funciones["modulo"] = x => (x >= 0) ? x : -x;
funciones["cuadratica"] = x => x ** 2;
